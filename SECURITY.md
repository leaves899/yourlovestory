# Security Policy / 安全政策

## Supported Versions / 支持版本

<!-- supported-version-policy: current-prerelease-only -->

项目仍处于 Alpha 阶段，不承诺长期支持周期。唯一受支持的版本是
`package.json` 指定的当前预发布版本；发布下一预发布版本后，上一版本即停止支持。

| Version | Supported |
| --- | --- |
| Current version from `package.json` | :white_check_mark: |
| Earlier prereleases and untagged builds | :x: |

安全修复通常进入新的预发布版本，不回补已被替代的预发布版本。Stable 版本发布后，
支持范围将在不超出维护能力的前提下重新评估，并以本文件为准。

## Reporting a Vulnerability / 报告漏洞

请使用仓库的 GitHub Security Advisories 私密报告漏洞。不要在公开 Issue、
Discussion、日志或截图中披露漏洞细节、API Key、数据库或用户数据。

仓库目前没有经过验证的安全邮箱，因此不提供虚假或占位邮箱。维护者会在能力范围内
确认报告、评估影响并协调修复，但不承诺固定响应时限。

在修复版本和公告准备完成前，请勿公开披露。完整的版本与发布规则见
[版本策略](docs/release/versioning.md)。
